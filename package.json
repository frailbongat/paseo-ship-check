{
  "name": "paseo-ship-check",
  "description": "Ship readiness for Paseo: the daemon checks the tree when a turn ends and drops a verdict card in the agent timeline, with a one-tap ship when the branch is clean.",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@getpaseo/plugin": "^0.8.0",
    "@tanstack/react-query": "^5.90.11",
    "@types/node": "^22.20.2",
    "@types/react": "~19.2.0",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "typescript": "^5.9.3",
    "zod": "^4.4.3"
  }
}
